language: generic
sudo:required
services:
  - docker


before_install:
  - docker build -t Yakium/docker-react -f Dockerfile.dev .

script:
  - docker run Yakium/docker-react npm run test -- --coverage